---
import { ChevronRight, CircleCheck } from "@lucide/astro";
import { Image } from "astro:assets";
---

<!-- --- PRE-LAUNCH CTA & FORM --- -->
<section id="registro" class="py-24 bg-slate-900 relative overflow-hidden">
    <!-- Abstract shapes -->
    <div
        class="absolute top-0 right-0 -mr-20 -mt-20 w-96 h-96 rounded-full bg-blue-600 opacity-20 blur-3xl"
    >
    </div>
    <div
        class="absolute bottom-0 left-0 -ml-20 -mb-20 w-80 h-80 rounded-full bg-cyan-500 opacity-10 blur-3xl"
    >
    </div>

    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <div
            class="bg-white rounded-3xl shadow-2xl overflow-hidden md:flex min-h-150"
        >
            <!-- Left: Info & Image -->
            <div
                class="relative p-8 md:p-12 md:w-5/12 text-white flex flex-col justify-center bg-slate-900"
            >
                <div class="absolute inset-0">
                    <!-- <img
                        src="/2.webp"
                        alt="Meeting"
                        class="w-full h-full object-cover opacity-60"
                        loading="lazy"
                    /> -->
                    <Image
                        src={"/2.webp"}
                        alt="Modern Architecture Building"
                        width={1920}
                        height={1080}
                        class="w-full h-full object-cover"
                        loading="lazy"
                    />
                    <div
                        class="absolute inset-0 bg-slate-900/40 mix-blend-multiply"
                    >
                    </div>
                    <div
                        class="absolute inset-0 bg-linear-to-tr from-slate-900/90 to-transparent"
                    >
                    </div>
                </div>

                <div class="relative z-10">
                    <span
                        class="font-bold tracking-wider text-xs uppercase bg-white/10 backdrop-blur-sm border border-white/20 px-3 py-1 rounded-full mb-6 inline-block shadow-sm"
                        >Exclusivo</span
                    >
                    <h2 class="text-3xl font-bold mb-4">Acceso Anticipado</h2>
                    <p class="text-slate-200 mb-8 leading-relaxed">
                        "El futuro inmobiliario se construye hoy. Únete a la
                        lista de fundadores y lidera el cambio."
                    </p>

                    <ul class="space-y-5">
                        <li
                            class="flex items-start gap-3 text-sm text-slate-200"
                        >
                            <CircleCheck
                                class="w-5 h-5 text-green-400 mt-0.5 shrink-0"
                            />
                            <span>1 mes de bonificación en destacados.</span>
                        </li>
                        <li
                            class="flex items-start gap-3 text-sm text-slate-200"
                        >
                            <CircleCheck
                                class="w-5 h-5 text-green-400 mt-0.5 shrink-0"
                            />

                            <span>Badge de "Early Adopter" en tu perfil.</span>
                        </li>
                        <li
                            class="flex items-start gap-3 text-sm text-slate-200"
                        >
                            <CircleCheck
                                class="w-5 h-5 text-green-400 mt-0.5 shrink-0"
                            />
                            <span
                                >Acceso prioritario a nuevas funcionalidades.</span
                            >
                        </li>
                    </ul>
                </div>
            </div>

            <!-- Right: Form -->
            <div
                class="p-8 md:p-12 md:w-7/12 bg-white flex flex-col justify-center"
            >
                <!-- Success Message (Hidden by default) -->
                <div
                    id="success-view"
                    class="hidden h-full flex-col items-center justify-center text-center fade-in"
                >
                    <div
                        class="w-20 h-20 bg-green-100 text-green-600 rounded-full flex items-center justify-center mb-6"
                    >
                        <CircleCheck />
                    </div>
                    <h3 class="text-3xl font-bold text-slate-900 mb-3">
                        ¡Gracias por sumarte!
                    </h3>
                    <p class="text-slate-600 text-lg max-w-sm mx-auto">
                        Tu registro está confirmado. Pronto recibirás novedades
                        de MPA y Tandil Solution.
                    </p>
                </div>

                <!-- The Form -->
                <div id="form-view" class="max-w-md mx-auto w-full">
                    <div class="mb-8">
                        <h3 class="text-2xl font-bold text-slate-900">
                            Completa tu perfil
                        </h3>
                        <p class="text-slate-500 mt-2">
                            Asegura tu lugar en el ecosistema digital.
                        </p>
                    </div>

                    <form
                        method="POST"
                        data-netlify="true"
                        netlify-honeypot="bot-field"
                        id="contact-form"
                        class="space-y-5"
                    >
                        <!-- Campo oculto para Netlify -->
                        <input type="hidden" name="form-name" value="contact" />

                        <!-- Campo honeypot para prevenir spam -->
                        <p class="hidden">
                            <label>
                                Don't fill this out if you're human: <input
                                    name="bot-field"
                                />
                            </label>
                        </p>

                        <div class="grid sm:grid-cols-2 gap-5">
                            <div>
                                <label
                                    class="block text-sm font-medium text-slate-700 mb-1"
                                    for="name">Nombre Completo</label
                                >
                                <input
                                    id="nombre"
                                    name="nombre"
                                    required
                                    type="text"
                                    class="w-full px-4 py-3 rounded-lg border border-slate-300 bg-slate-50 focus:bg-white focus:ring-2 focus:ring-blue-600 focus:border-transparent outline-none transition-all"
                                    placeholder="Ej. Juan Pérez"
                                />
                            </div>
                            <div>
                                <label
                                    for="inmobiliaria"
                                    class="block text-sm font-medium text-slate-700 mb-1"
                                    >Inmobiliaria</label
                                >
                                <input
                                    id="inmobiliaria"
                                    name="inmobiliaria"
                                    required
                                    type="text"
                                    class="w-full px-4 py-3 rounded-lg border border-slate-300 bg-slate-50 focus:bg-white focus:ring-2 focus:ring-blue-600 focus:border-transparent outline-none transition-all"
                                    placeholder="Ej. Inmobiliaria Pérez"
                                />
                            </div>
                        </div>

                        <div>
                            <label
                                for="email"
                                class="block text-sm font-medium text-slate-700 mb-1"
                                >Email Profesional</label
                            >
                            <input
                                id="email"
                                name="email"
                                required
                                type="email"
                                class="w-full px-4 py-3 rounded-lg border border-slate-300 bg-slate-50 focus:bg-white focus:ring-2 focus:ring-blue-600 focus:border-transparent outline-none transition-all"
                                placeholder="juan@inmobiliaria.com"
                            />
                        </div>
                        <div>
                            <label
                                for="ciudad"
                                class="block text-sm font-medium text-slate-700 mb-1"
                                >Ciudad / Localidad</label
                            >
                            <input
                                id="ciudad"
                                name="ciudad"
                                required
                                type="text"
                                class="w-full px-4 py-3 rounded-lg border border-slate-300 bg-slate-50 focus:bg-white focus:ring-2 focus:ring-blue-600 focus:border-transparent outline-none transition-all"
                                placeholder="Ej. Tandil"
                            />
                        </div>

                        <button
                            id="submit-btn"
                            type="submit"
                            class="w-full mt-4 bg-blue-700 text-white font-bold py-4 rounded-lg hover:bg-blue-800 transition-all flex items-center justify-center gap-2 disabled:opacity-70 disabled:cursor-not-allowed shadow-lg shadow-blue-700/20"
                        >
                            <span>Asegurar mi lugar</span>

                            <ChevronRight />
                        </button>
                        <p class="text-xs text-center text-slate-400 mt-4">
                            Tus datos están protegidos. No enviamos SPAM.
                        </p>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>
